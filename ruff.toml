# ruff.toml

# Only lint the new code:
select = ["E", "F", "I", "B"]
line-length = 100
target-version = "py311"

# Don’t touch the legacy folders (now consolidated under legacy/):
exclude = ["legacy"]
